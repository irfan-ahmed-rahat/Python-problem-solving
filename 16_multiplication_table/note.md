"""
Problem 16: Print the multiplication table of a given number.
"""

n = int(input("Enter number: "))

print(f"\n--- Multiplication Table of {n} ---")

#১ থেকে ১০ পর্যন্ত লুপ ঘুরবে

for i in range(1, 11):

#n এর সাথে সিরিয়াল নম্বর i সরাসরি গুণ করা হচ্ছে

    result = n * i
    
#ফলাফলটি লুপের ভেতরেই সুন্দর ফরম্যাটে প্রিন্ট করা হচ্ছ
ে
    print(f"{n} * {i} = {result}")





############################################

প্রিন্ট ফাংশন টি বার বার 
ফলাফলের প্রিন্ট গুলো দেখাচ্ছে কেন ?

কারণ এটাকে লুপের ভেতর ঢুকিয়ে দেওয়া হয়েছে ।
লুপের বাইরে রাখলে শুধু একবার ওই এটা প্রিন্ট করবে এবং ফাইনাল রেজাল্ট দেখাবে ।

মানে print() ডান পাশে একটু সরিয়ে লুপের ভেতরে ঢুকানো হয়েছে

################################################
 
 প্রবলেম ১৬: একটি সংখ্যার নামতা (Multiplication Table) প্রিন্ট করো

বেসিক নলেজ এবং ডেফিনেশন

নামতা (Multiplication Table) কী?


নামতা হলো কোনো একটি নির্দিষ্ট সংখ্যাকে ১ থেকে ১০ (বা তার বেশি) পর্যন্ত সিরিয়ালি গুণ করে তার ফলাফলগুলো পর পর সাজিয়ে দেখানো।

যেমন, ৫ এর নামতা:

৫ × ১ = ৫

৫ × ২ = ১০ ... ইত্যাদি।


ডেফিনেশন: f-string হলো পাইথনে টেক্সট (String) প্রিন্ট করার একটি আধুনিক ও সহজ উপায়। 

কোনো টেক্সটের শুরুতে ছোট হাতের f লিখে, কোটেশনের ভেতর {} (কার্লি ব্রেস বা দ্বিতীয় বন্ধনী) দিয়ে সরাসরি যেকোনো ভেরিয়েবল বা হিসাব বসিয়ে দেওয়া যায়।

কেন দরকার:

আপনি যদি 5 x 1 = 5 আউটপুটটি হুবহু অঙ্কের বইয়ের মতো সুন্দর করে প্রিন্ট করতে চান, 

তবে সাধারণ উপায়ে কমা (,) দিয়ে ভেরিয়েবল ও টেক্সট জোড়া লাগানো বেশ ঝামেলার। f-string এই কাজটিকে একদম পানির মতো সহজ করে দেয়।

যেমন: print(f"{n} x {i} = {result}")।

 বাস্তব জীবনে, বিশেষ করে কোনো দোকান বা ই-কমার্স সাইটের ইনভয়েস (Invoice) বা বিলিং সিস্টেমে এই লজিকটি খুব ব্যবহার হয়।
 
 ধরুন, একটি শার্টের দাম ৫০০ টাকা।
 
 কোনো কাস্টমার যদি একাধিক পিস কিনতে চায়, তবে সিস্টেমে ১ পিসের দাম ৫০০, ২ পিসের দাম ১০০০,
 
 ৩ পিসের দাম ১৫০০—এভাবে একটি ডায়নামিক প্রাইস বা রেট-লিস্ট দেখানোর জন্য লুপের সাথে গুণের এই কনসেপ্টটি কাজে লাগানো হয়।

প্রিন্ট কে এখানে for loop এর অংশ করা হয়েছে ফলে সবগুলো প্রদর্শন করছে । 

এখানে print ফাংশনটি লুপের ভেতরে (Space বা Tab দিয়ে একটু ডানদিকে সরিয়ে) লেখা হয়েছে

এর মানে হলো, print এখন লুপের নিজস্ব কাজের একটি অংশ!

তাই লুপ যতবার ঘুরছে (১০ বার), সে প্রথম লাইনে গুণ করছে এবং দ্বিতীয় লাইনেই সাথে সাথে সেটি স্ক্রিনে প্রিন্ট করে দিচ্ছে।

 আগেরগুলোতে এখানে print ফাংশনটি লুপের সীমানার বাইরে ছিল।
 
 তাই লুপটি ভেতরে ভেতরে তার সব গুণের কাজ শেষ করে যখন পুরোপুরি থেমে গেছে,
 
 কেবল তখনই সে লুপ থেকে বের হয়ে ফাইনাল রেজাল্টটি মাত্র একবার প্রিন্ট করেছে।


 
